extends layout

block content
  #wrapper(ng-controller="mapCtrl" ng-init="init()")
    canvas#main(resize)
    #toolbox
      #pause(ng-click="togglePause()") ||
      .slave(ng-repeat="s in slave_names" color="{{slaves[s]['color']}}" 
             slave="{{s}}" ng-click="chooseSlave(s)")
      #remaining_slave 5
  script

